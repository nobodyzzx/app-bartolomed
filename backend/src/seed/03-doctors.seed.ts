import { DataSource } from 'typeorm';

// Seed removido - usar API para crear doctores reales
export async function seed(_dataSource: DataSource) {
  // Seed deshabilitado: usar API para crear doctores reales
}
