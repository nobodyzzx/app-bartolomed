import { DataSource } from 'typeorm';

// Seed removido - usar API para crear prescripciones reales
export async function seed(_dataSource: DataSource) {
  console.log('ℹ️ Prescription seed disabled - use API to create real prescription data');
}
