import { DataSource } from 'typeorm';

// Seed removido - usar API para crear citas reales
export async function seed(_dataSource: DataSource) {
  // Seed deshabilitado: usar API para crear citas reales
}
